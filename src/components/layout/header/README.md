# Header Component

A glassmorphism-styled header component that responds to scroll events.

## Props

- `isScrolled`: boolean - Whether the page has been scrolled

## Usage

```tsx
import { Header } from '@/components/layout/header/Header';

<Header isScrolled={isScrolled} />;
```
