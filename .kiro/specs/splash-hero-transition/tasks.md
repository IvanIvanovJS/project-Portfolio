# Implementation Plan

- [x] 1. Create splash screen component structure
  - Create `src/components/splash/SplashScreen.tsx` component with basic structure
  - Create `src/components/splash/SplashScreen.module.css` with container and text styling
  - Implement fixed overlay with z-index 9999 to cover all content
  - Add Rubik Glitch font import and styling for main text
  - Add IBM Plex Sans italic styling for subtext
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 4.1, 4.2, 4.3, 4.4, 4.5_

- [x] 2. Implement text animation sequence
  - [x] 2.1 Create CSS keyframes for slide animations
    - Write `@keyframes slideInLeft` for "Assembling" text
    - Write `@keyframes slideInRight` for "Technical Stack" text
    - Write `@keyframes slideInUp` for "Compiling innovation..." text
    - Apply cubic-bezier easing for smooth motion
    - _Requirements: 1.2, 1.3, 1.4, 2.2_
  - [x] 2.2 Implement animation timeline with React state
    - Create state machine for animation phases (text-in, hold, text-out, glow, complete)
    - Use setTimeout to schedule each text element appearance
    - Implement 0.3s hold period after all text appears
    - Store timer references for cleanup
    - _Requirements: 1.5, 2.4_

- [x] 3. Create fade-out and leak glow effects
  - [x] 3.1 Implement text fade-out animation
    - Write `@keyframes fadeOut` with translateY(-40px) and scale(0.95)
    - Apply fade-out to all text elements simultaneously
    - Set 0.5s duration with cubic-bezier easing
    - _Requirements: 2.1, 2.2_
  - [x] 3.2 Create leak motion glow effect
    - Add leak glow element with radial gradient background
    - Write `@keyframes glowExpand` to animate from center to full width
    - Position glow to start at 80% of fade-out completion
    - Apply blur filter for light streak effect
    - _Requirements: 2.2, 2.4_

- [x] 4. Implement hero section reveal transition
  - Write `@keyframes heroReveal` with fade-in and translateY(60px to 0)
  - Add transition class to splash container for opacity fade
  - Remove splash from DOM after transition completes
  - Ensure smooth 0.7s reveal duration
  - _Requirements: 2.3, 2.4, 2.5_

- [x] 5. Create asset preloading hook
  - [x] 5.1 Implement useHeroPreload hook
    - Create `src/hooks/useHeroPreload.ts` file
    - Track ThreeScene asset loading status
    - Monitor icon atlas texture loading
    - Check Rubik Glitch font loading via document.fonts API
    - Return combined loading status
    - _Requirements: 3.1, 3.2, 3.3_
  - [x] 5.2 Integrate preload hook with splash timing
    - Call useHeroPreload in SplashScreen component
    - Wait for both animation completion AND asset loading
    - Implement 5-second timeout fallback for slow connections
    - Extend hold period if assets not ready
    - _Requirements: 3.3, 3.4, 3.5_

- [x] 6. Integrate splash screen into layout
  - [x] 6.1 Add Rubik Glitch font to layout head
    - Add Google Fonts preconnect links in `src/app/layout.tsx`
    - Add Rubik Glitch font stylesheet link with display=swap
    - _Requirements: 4.2, 6.5_
  - [x] 6.2 Render splash screen outside LayoutWrapper
    - Import SplashScreen component in layout.tsx
    - Render SplashScreen as sibling to LayoutWrapper (not inside it)
    - Ensure splash covers navigation and all content
    - Verify no layout disruption or blinking occurs
    - _Requirements: 6.1, 6.2, 6.3, 6.4_

- [ ] 7. Implement accessibility features
  - [ ] 7.1 Add reduced motion support
    - Detect prefers-reduced-motion media query
    - Create reduced motion timeline with 0.1s durations
    - Remove transform animations, keep only fade transitions
    - Reduce total duration to under 1 second
    - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5_
  - [ ] 7.2 Add ARIA attributes and screen reader support
    - Add role="status" and aria-live="polite" to splash container
    - Add aria-label="Loading application"
    - Hide visual text with aria-hidden="true"
    - Add screen reader only text with complete message
    - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5_

- [ ] 8. Add responsive design and mobile optimizations
  - Create mobile breakpoint styles (max-width: 768px)
  - Reduce main text font size from 3rem to 2rem on mobile
  - Reduce subtext font size from 1.2rem to 1rem on mobile
  - Add horizontal padding to text container on mobile
  - Prevent scroll and touch interactions during splash
  - _Requirements: 4.1, 4.4, 4.5_
